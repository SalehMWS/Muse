import { RegisterForm } from "@/features/auth/components/register-form";

export const metadata = { title: "Create account | NovaFlow" };

export default function RegisterPage() {
  return <RegisterForm />;
}
